# Air5 Mach 4 低压 SBLI 推进方案

日期：2026-09-24。工况替换已经用户确认。保留旧 Mach 6 映射工况与失败证据。

## 2026-09-25 冻结修复目标验收

对称通量候选的本轮目标关闭，默认仍为 `full_state`。保留 N-1 独立组成，
五组分对称构造/投影通量，总质量与双温能量联合约束；CPU/GPU 实际 RK
及 MPI 已接入。没有经验地板、状态截零、事后归一化或 N2 余量覆盖。
时序审查未证实需新增快照的问题，因此未增加阶段状态副本。

| 冻结要求 | 最终证据与结果 |
| --- | --- |
| 接触平衡、五幅值、低 N2、保正与双温、CPU NP1/NP2/GPU NP2 | `out/air5_symmetric_final_*_20260925/summary.json`：48 次候选及 3 次旧默认通过，含实际 x/y/z 分解。最大 p/T/u 漂移 2.473826e-10 Pa / 8.185453e-12 K / 9.521273e-13 m/s |
| 界面约束激活、逐组分误差、守恒 | 同一矩阵：能量/组分约束在 MPI 界面激活，1440 对完整面逐位一致，逐组分自身尺度误差最大 1.571109e-15，周期守恒残差最大 2.920763e-14；严格零组分保持零 |
| 120 种对称置换及失败拒绝 | CPU `air5_transport_roundoff_probe` 和设备 `air5_flux_gpu_probe` 通过，包括退化约束、痕量、不可行输入；`out/air5_flux_gpu_final_memcheck_20260925.log` 零错误 |
| 对称缓存 | 五种痕量组分的零及非零幅值、守恒状态不变与旧默认恢复通过；`out/air5_cache_final_memcheck_20260925.log` 零错误 |
| 实际主循环内存安全 | 最终矩阵 32 份候选 rank memcheck 日志零错误，旧默认另 2 份零错误；非周期 GPU 单步/半步回放同样通过 |
| 非周期对流守恒 | `out/air5_symmetric_boundary_balance_{cpu,gpu}_20260925/boundary_flux_balance.json`：三阶段体 RHS 与净边界通量相符，残差最大 1.389391e-16，共享面逐位一致 |
| 同检查点 CPU/GPU 与匹配时间 dt/2 | `out/air5_symmetric_boundary_balance_gpu_20260925/cpu_gpu.json` 的 20 份场通过原容差；`out/air5_symmetric_gpu_boundary_half_20260925/matched_window.json` 给出同终点全部守恒分量、痕量及原始量差，最大 u 差 2.883809e-5 m/s |
| 检查器 | 52 项相关 Python 测试通过；人工构造净通量错误及共享面逐位破坏均被拒绝 |

上述 `out/` 均位于 `tests/gpu_validation/`。最终主程序 SHA256 为
`0d7e789969af9825d6185c73fb23b792c93129b5e14239f0b7700ea7ce253883`。
dt/2 回放采用前一构建，其后仅增加 CPU 单轴非周期面记录，不改变
GPU 算术；该对照内部两次运行的可执行文件与检查点完全相同，契约已检查。
后续长窗口、入射激波及更广边界族验证属于新的工作，不从本轮结果推断
长时物理闭合，不提升默认。下文保留实施历史。

## 2026-09-25 对称缓存阶段进展

GPU 后续进展：已实现与 CPU 一致的单侧物理面预算、固定节点排除及
MPI_PROC_NULL 处理，替换此前的非周期拒绝。原检查点一次 2 ns GPU
回放与 CPU 的 20 份同相位场通过原逐元素容差，最大缩放差 1.354173e-14。
GPU 一次 2 ns/两次 1 ns 到达同一 t=2.182 us，18/36 份阶段状态有效，
四份 rank memcheck 日志零错误。入口 rank 最大流向速度差为
2.883809e-5 m/s，T/p 差为 5.766585e-5 K/2.507619e-3 Pa；旧方案相同
短窗口速度差约 0.429740 m/s。此处只能说明组合修订后的短时改善。
证据：`out/air5_symmetric_gpu_boundary_{late,half}_20260925/`，其中
`half` 的 `matched_window.json` 逐分量报告全部守恒量及痕量误差。
开放边界通量收支与最终完整矩阵仍未关闭，不提升默认、不跑长时生产。

最新进展：CPU 单侧物理边界预算已实现候选，GPU 非周期拒绝仍保留。
实际固定边界节点不在 `is:ie/js:je/ks:ke` 推进区。物理面只检查域内
状态，内部面仍检查两侧；通过质量约束推导缺失侧界，不使用状态截零。
MPI_PROC_NULL 对应面不执行周期本地复制、不用空接收覆盖物理面。
8 次 CPU 周期回归通过，原 step1090 检查点一次 2 ns NP2 CPU 回放完成，
18 份阶段状态通过原状态检查，最大质量闭合相对残差 9.002067e-16。
结果为 `out/air5_symmetric_cpu_boundary_late_20260925/`，不是完整物理边界
验收；剩余 GPU 实现、开放边界通量收支、同相位/dt/2 及内存安全门槛。

后续更新：实际 y/z slab 验证已完成。`run_air5_layered_stress_gate.py`
新增 `--axis`，旋转物理输入并将诊断还原到统一方向，保持原数值门槛。
两方向共 24 次 CPU NP1/NP2、GPU NP2 运行通过，720 对面逐位一致，
16 份 memcheck rank 日志零错误，50 项检查器测试通过。证据为
`tests/gpu_validation/out/air5_symmetric_{y,z}_slab_20260925/`。
下述剩余顺序中的 y/z 步骤已关闭，下一项为非周期单侧预算。

实验选项 `symmetric_species` 的 CPU/GPU 组成缓存采用所有五个
`rho_s/rho`，不再单独覆盖 N2，不修改守恒状态或旧默认。模式在初始化
物性查询前配置，设备内点、边界准备、化学入口使用相同配置。
五组分痕量缓存探针 memcheck 通过；当前主程序 24 次候选及 3 次默认
对照通过周期冻结门槛，候选 720 对共享面逐位一致，16 份设备 rank 日志
零错误。最大守恒残差 2.534431e-15，逐组分自身尺度误差 1.571109e-15。
证据见 `out/air5_symmetric_cache_primary_20260925/` 与
`out/air5_cache_scan_*_20260925/`（位于 `tests/gpu_validation/`）。

剩余顺序：先扩展测试脚本的轴向场拼接和输入，完成真实 y/z 分解；再
实现物理边界单侧预算和最终界面通量共享，保持现有边界物理不变；最后
进行既定 Mach4 同 checkpoint 短回放及 dt/2 对照。当前物理边界明确拒绝
新选项，不能直接删除拒绝条件：已有共享函数假定两端周期邻居，预算
还假定界面两侧均有推进状态。开放边界守恒须计入边界通量，不能照搬
周期全域总量不变的判据。不增加长时生产计算，不宣布物理闭合。

## 工况与用途

本算例是固定五组分双温模型的工程耦合验证，不是指定论文的严格复现。
无激波平板由 ASTR 主程序预发展。Python 只准备数据、检查状态和管理运行，
不另建完整流动推进器。原压力域失败见
[诊断记录](ASTR_AIR5_SBLI_PRESSURE_DOMAIN_DIAGNOSIS.md)。

| 参数 | 设定 |
|---|---|
| 入口冻结声速马赫数 | 4 |
| 入口 T / Tv | 1500 / 1500 K |
| 入口压力 | 20000 Pa |
| 质量分数 N2/O2/N/O/NO | 0.767/0.233/0/0/0 |
| 入口速度 / 密度 | 3112.356772 m/s / 0.046248672 kg/m3 |
| 壁面 | 无滑移、非催化，T=Tv=3000 K |
| 入射激波角 | 相对入口边缘流向 25 度，不是压缩转角 |
| 冻结跃迁后的压力 / T | 63346.312859 Pa / 2177.259771 K |
| 冻结跃迁转角 | 12.937449 度 |
| 展向 | 周期、层流三维挤出 |
| 数值路径 | FP64、显式同步、现有 LLF/MP7、耦合源项、黏性、关闭滤波 |
| 本地并行 | NP=2，2x1x1，一 rank 一 GPU |

冻结规则反射压力估算约 0.1603 MPa，只是选型余量检查，不是黏性反应流峰值
保证。保留代码原有 1 MPa 压力上限及 300--8000 K 温度范围，此外新工况诊断
要求平动温度不低于 1000 K，以避免已有二元扩散拟合的低温外推。
换工况不能解决既有平衡常数拟合与热力学闭合之间的一致性问题；化学平衡
物理验证仍独立待做。不得把新短步通过写成这些模型问题已经解决。

## 起步网格与入口

`prepare_air5_mach4_case.py` 独立准备 `air5hbl`，不会打开激波顶面。
初始速度是有限厚度四次多项式种子：`f(s)=2s-2s^3+s^4`，`0<=s<=1`，
外部 `f=1`。`u=Uinf*f`，`v=w=0`，`T=Tv=Tw+(Tinf-Tw)*f`，
`rho=pinf/(Rmix*T)`，组分保持入口组成。
这不是可压缩双温相似解，必须先经 ASTR 预发展。

启动剖面以 `Re_delta_star=1000` 定标，位移厚度定义为
`integral[1-rho*u/(rho_inf*Uinf)]dy`。当前模型边缘黏度为
`5.40477883e-5 Pa s`，所得参考厚度 `0.37548214 mm`，种子总厚度
`0.97641532 mm`。预发展域 `80x12x2` 倍参考厚度，即
`30.03857 x 4.50579 x 0.750964 mm`。流经时间 `Lx/Uinf=9.65139 us`。
这些是起步参数，不是网格或入口物理收敛结论。

首轮网格上界 `63,63,7`，步长 `2e-9 s`。CFL 以实际输出为准，达到 1
或出现实际负组分、NaN、越界即停止。先进行短步 CPU/GPU 同相位比较，
再进行不超过一个流经时间的预发展试跑。确认趋势后才安排多流经时间收敛。
必须覆盖位移厚度、壁压和壁热通量的时间变化，不能仅凭任务正常退出判断稳态。

`air5_hbl_domain.dat` 是可选尺寸接口，首行为 `air5_hbl_domain_v1`，
第二行为 SI 单位的 Lx/Ly/Lz。无此文件时保留旧 air5hbl/air5sbli 的尺寸。
当前只用于内置 Cartesian 网格，不宣称验证外部曲线网格。

## 推进顺序与停止条件

1. 新配置数据检查、旧配置回归、根 CMake 编译。
2. 新平板 CPU/GPU 短步场、原始传感器、掩码与模型有效域检查。
3. 双 GPU 无激波预发展，保存完整 RK checkpoint 和少量统计，检查 CFL。
4. 多流经时间、步长和网格敏感性检查。提取 ASTR 自身演化后的剖面与初场，
   保留全组分、Tv 和法向速度。裁剪/移站后必须重新匹配入口、顶面和流向坐标。
5. 以实际边缘状态重新生成冻结入射跃迁，再做小规模 CPU/GPU 激波接入。
6. 通过后推进有界长时 SBLI；原始痕量/展向问题保留独立诊断记录。

计划顶面转折为 x=20 倍参考厚度。零入口偏转时几何落点约 x=45.734 倍
参考厚度；理想规则反射波重新到达顶面的 x 约为 85.8 倍参考厚度，超过
当前域长 80。实际预发展边缘法向速度、波系和边界层增厚会改变此估算，
必须在激波接入前重算，不允许预先认定完整状态顶面适用于最终波系。

禁止直接用解析种子或缩放旧 HTR 初场开始长时 SBLI 并称为物理验证。
新出现的 CPU/GPU 不一致或模型域错误仍是停止条件，不能援引旧展向误差豁免。

## 可复现入口

数据准备：
```bash
python3 tests/gpu_validation/prepare_air5_mach4_case.py \
  --destination tests/gpu_validation/out/mach4_precursor_seed \
  --grid 63,63,7 --maxstep 19 --deltat 2.d-9 --use-gpu t
```

短步同相位门槛通过后，有界预发展：
```bash
python3 tests/gpu_validation/run_air5_sbli_long.py \
  --case mach4-precursor --grid 63,63,7 --updates 1001 --dt 2e-9 \
  --checkpoint 100 --timeout 3600 \
  --output tests/gpu_validation/out/mach4_precursor_pilot
```

该命令只推进约 0.207 个流经时间，不能单独给出充分发展结论。
每次使用新的输出目录，禁止覆盖旧证据。

## 本轮结果

根 CMake CUDA/air5 编译通过，36 项数据准备/状态监控测试通过。
尺寸接口的错误头、负尺寸和 NaN 均由双 rank CPU 启动明确拒绝。
无尺寸文件的旧 SBLI 双卡回归通过：36 组场，最大缩放差 `8.51e-16`，
原始传感器及掩码一致。没有修改原壁面闭合、输运公式或模型有效域。

| 验证 | 本轮结果 |
|---|---|
| 新平板短步 | NP=2，63,63,7，20 次更新，dt=2e-9 s |
| CPU/GPU 同相位 | 18 组场，绝对差最大 2.33e-10，缩放差最大 3.35e-14 |
| 接受阈值 | 逐元素 1e-9 + 1e-10 abs(q)，不是把原始相对差用于近零分量 |
| 传感器 | 6 组，原始值最大差 5.11e-15，掩码相同 |
| 无激波预发展试跑 | 1001 次更新，dt=2e-9 s，正常退出，约 163.5 s 墙钟时间 |
| 完整 RK checkpoint | step 1000，t=2.000 us；末次更新结束 t=2.002 us |
| 最大 CFL | 0.067698 |
| 末步 18 组阶段快照 | T 1499.9597--3000 K；p 19.68986--21.46382 kPa |
| 正性/闭合 | 最小部分密度 0；阶段快照组分和最大相对残差 9.36e-14 |
| 末 checkpoint 展向诊断 | max(abs(w))/Uinf=1.38174e-9；压力展向 rms/pinf=2.72158e-10 |

上表墙钟时间不是性能基准。运行期间 nvitop 确认两个 ASTR rank 分别占用
GPU 0/1，单次观测利用率约 45%/59%。服务正常结束，无继续后台长跑。

末 checkpoint 的 N、O、NO 最大质量分数分别约为 `8.10e-11`、`8.07e-7`、
`6.61e-10`，说明当前时段化学作用较弱，不能据此完成强反应 SBLI 验证。
约 x/Lx=0.25/0.5/0.75 的位移厚度为初始值的
1.02108/1.02329/1.02329 倍，域内仍大范围受初始种子控制。
展向扰动增长已记录，不把其归因确定为舍入误差，也未接受其长期稳定性。

当前结论为有界预发展状态检查通过，不是充分发展、网格收敛或 SBLI 物理通过。
下一步仍是多流经时间平板、壁面响应和分辨率检查，再提取匹配入口及初场。
本轮没有施加入射激波。

证据目录（相对 `tests/gpu_validation/`）：

- `out/air5_mach4_precursor_gate_20260924/`：CPU/GPU 日志、场、逐元素比较。
- `out/air5_mach4_precursor_pilot_20260924/`：执行清单、不可变可执行文件、
  `statistics.jsonl`、`status.json`、`field_review.json`、完整 checkpoint。
- `out/air5_sbli_domain_interface_regression_20260924/`：旧配置回归。
- `out/air5_hbl_domain_rejection_20260924/`：尺寸输入失败门槛。

## 续算与时间演化检查

step 1000 同一完整 checkpoint 的 CPU/GPU 单步重放通过，18 组场最大
缩放差 `1.86e-12`。GPU 重启与原连续运行相同步骤/阶段比较也通过，18 组场
最大缩放差 `2.95e-12`，两组比较的传感器掩码均一致。改变 dt 为 `1e-9 s`
的两次更新另外检查了续算时钟：checkpoint step 1001 对应 `2.001 us`，
不能误用全局 step 乘新 dt 得到 `1.001 us`。

`run_air5_sbli_long.py --baseline` 从不可变的案例归档新建续算目录，要求
可执行文件和机理与基线一致，并继承网格。原 checkpoint 和初场不覆盖。
流动仍由 ASTR 完整推进，Python 只管理任务、保存完整 RK checkpoint 和诊断。

```bash
python3 tests/gpu_validation/run_air5_sbli_long.py \
  --case mach4-precursor \
  --baseline tests/gpu_validation/out/air5_mach4_precursor_pilot_20260924/gpu \
  --executable tests/gpu_validation/out/air5_mach4_precursor_pilot_20260924/astr \
  --updates 4001 --dt 2e-9 --checkpoint 500 --timeout 1800 \
  --output tests/gpu_validation/out/mach4_ft1_new
python3 tests/gpu_validation/check_air5_precursor_evolution.py \
  --output tests/gpu_validation/out/mach4_ft1_new
```

目标完整 checkpoint 为 step 5000、`10.000 us`，末更新为 `10.002 us`，
约 1.036 个流经时间。先检查一个流经时间的变化，再决定多流经时间计算。

诊断在固定 x/Lx 约 0.25/0.5/0.75 的节点上进行。位移厚度采用
`integral(1-mean_z(rho*u)/mean_z(rho*u)_edge)dy`，周期端点只计一次。
保存温度、Tv、速度、密度、压力和全部组分剖面。
壁面摩擦系数为 `2*mu_w*du_dy_w/(rho_inf*Uinf^2)`，导热为
`-k_tr*dT_dy-k_v*dTv_dy`，正值表示从下壁进入流体，单位 `W/m^2`。
非催化壁面法向组分扩散焓通量取已施加的零值。
后处理沿用已有七点单边导数权重，逐展向点计算后平均，属于独立壁面观测量，
不冒充求解器离散 RHS 中的边界通量，也没有修改壁面格式。

后处理只输出时间历史与最近窗口变化，不自动给出稳态/物理通过。
热流穿过零值时相对变化可超过 100%，须同时看带符号的绝对值。
早期热流由正转负与摩擦仍变化，表明不能只凭壁压波动较小提取稳定入口。

### 一个流经时间与法向加密结果

上述 4001 次续算正常结束，step 5000 完整 checkpoint 为 `10.000 us`，
末更新 `10.002 us`，约 1.036 个流经时间。最大 CFL `0.0710347`。
末步 18 组阶段快照的 T 为 `1488.882--3140.055 K`，p 为
`19.5948--23.2655 kPa`，无负部分密度，组分和最大相对残差 `3.07e-13`。
最后 checkpoint 的 `max(abs(w))/Uinf=4.26e-8`，过程中曾升高后回落。
这些仅是有界运行与状态检查，不是稳态或长期挤出稳定性通过。

壁面剖面已有锯齿状变化，单边导数结果对模板非常敏感。
例如 x 节点 16、step 5000，七点与三点诊断分别得到：

| 量 | 七点 | 三点 |
|---|---:|---:|
| Cf | 0.0220815 | 0.00302715 |
| 向流体热流 W/m2 | +2025851 | -122498 |

所以此前七点诊断的变号不能直接解释为真实壁面分离或传热方向改变。
三点诊断只是另一个敏感性对照，不能挑选较平滑的结果来宣布物理通过。
最后 `9--10 us`，节点 16 的三点 Cf 和热流变化仍约 44% 和 45%；
节点 32 的热流变化约 25%，尚不能提取稳态入口。

随后保持物理域、初始解析种子、dt、源项和边界不变，仅将网格上界从
`63,63,7` 加密为 `63,127,7`，双卡从 t=0 推进 1001 次更新。
加密计算正常结束，最大 CFL `0.0989005`，末步 18 组阶段状态检查通过。
比较严格取两个网格的 step 1000 完整 checkpoint，即相同 `t=2.000 us`，
不是用粗网格的 10 us 与细网格的 2 us 混比。

| 对比量 | 64 与 128 个法向点的差别 |
|---|---|
| 位移厚度，3 个站位 | 相对细网格约 0.154%--0.289% |
| 壁压，3 个站位 | 相对细网格约 1.058%--1.075% |
| 三点 Cf | 相对细网格约 18.56% |
| 三点壁面热流 | 相对细网格约 9.47%--9.96% |
| 七点 Cf / 热流 | 相对细网格约 107% / 181%，更敏感 |
| max(abs(w))/Uinf | 粗网格 1.38e-9；细网格 1.25e-14 |

以上百分比是两个网格的差，不是相对真解的误差。
细网格在 2 us 的展向扰动降低，不能推断其 10 us 或更长时间也已稳定。
新工况解决了本轮物性域覆盖问题，但近壁分辨率、导数闭合和启动调整仍需分离。
未据此认定 CPU 边界存在明确 bug，也未修改求解器格式。

下一步先做相同物理时间的时间步敏感性和进一步法向分辨率检查，并审查
入口启动与近壁输运离散的相容性。壁面量尚敏感时，不盲目追加多流经时间，
不施加入射激波，不形成 SBLI 物理结论。涉及 CPU 格式修复时仍须人工决策。

本轮证据位于 `tests/gpu_validation/out/`：

- `air5_mach4_late_restart_gate_20260924/`：晚期后端与重启连续性对照。
- `air5_mach4_restart_clock_20260924/`：改变 dt 的续算时钟检查。
- `air5_mach4_precursor_ft1_20260924/`：一个流经时间，时间历史与阶段场检查。
- `air5_mach4_precursor_y128_20260924/`：法向加密及 `grid_comparison.json`。

`precursor_evolution.json` 使用实际保存的质量通量先求展向平均，再积分位移厚度。
第一轮续算运行中的在线剖面诊断使用平均 rho 与平均 u 的乘积，该运行脚本已
按启动 hash 归档为 `driver_at_launch.py`；离线诊断已统一为严格质量通量定义。
两者不改变 ASTR 的状态推进，正式时间历史取离线重新计算的结果。

### 首点 y+ 与入口敏感性

已完成 [分辨率与入口审查](ASTR_AIR5_MACH4_RESOLUTION_INLET_AUDIT.md)。
128 点在 t=2 us 的首点 y+ 仍为 1.375--1.504（三点）或 1.566--2.489（七点）。
同一 checkpoint 出发的入口厚度 0.9/1.0/1.1 倍双卡短时对照全部正常结束。
在 t=3 us，近入口节点 2 的三点热流相对基准变化 +21.39%/-22.51%，
域中部则几乎无变化，仍不能关闭壁面导数模板敏感性或长时间入口相容性。
保持原格式与模型；后续同物理时间的 128/256/512 点加密和 dt/2 检查已完成。
首点 y+<=1 为设计参考，不替代层流壁面热流、剪切及两温/组分剖面收敛。
三组正式计算有界通过，但 y512 的近壁模板差异仍明显，入口首列的七点热流
还存在约 20%--22% 的时间步敏感性。当前不关闭平板/SBLI 物理门槛，
下一步先定位入口与近壁输运耦合，不自动修复 CPU 格式。完整数值见上述审查报告。

## 分层扩散后的当前停止点（2026-09-24）

按人工批准，CPU/GPU 已接入可选能量约束分层扩散 `layered`，默认仍保留
`full_state`。组分扩散及其携能先限幅，混合通量随后接受独立能量约束。
实际 ASTR 的能量/组分激活压力测试及 x-slab 接口检查已通过；CPU/GPU
逐场、晚期同状态回放也通过。压力测试不作为物理精度验证。

当前采用 `species_budget + layered`，从同一 y512、t=2 us checkpoint
完成 2/1/0.5 ns 三档双卡计算，更新次数为 100/200/400，同到 t=2.2 us。
全部正常退出，保存状态无负组分或模型越界，最大 CFL 为 0.28646 以下。
但入口附近最大 u 差从粗/中比较的 `2.51414 m/s` 增到中/细比较的
`3.27230 m/s`，并未随时间步细化而收缩。不能据小 CFL 或能跑通判定
平板已可信，更不能直接开始长期 SBLI 并称为物理验证。

推进顺序第 4 项的入口时间敏感性仍未关闭，因此本轮未进入第 5/6 项。
下一步固定同一物理模型，针对入口附近有限窗口做累计算子收支，核查
对流保正限制与精确零组分/规定入口的耦合。任何新通量方案或边界物理
改动先提交人工决策，不通过加浓度地板、截零或降低验收要求绕过。
算例、数值及文件记录见
[分辨率与入口审查](ASTR_AIR5_MACH4_RESOLUTION_INLET_AUDIT.md)。

后续局部定位已确认：同一 t=2.18 us 状态的一次 2 ns/两次 1 ns 回放中，
NO 负预算和全分量共用对流面系数贡献入口速度差的主要部分，CPU/GPU 一致。
该点未受限高阶对流的 NO 仍为正，表明现有充分保正条件在当地偏保守，
不是取消全域保正的依据。只读探针和端点收支证据已补入入口审查文档。
下一步先审定对流限制改进的质量通量闭合与两温能量约束，再做原检查点
短回放及同物理时间的三档时间步验证；不直接恢复长时 SBLI。

**上述改进已获批准并完成候选实现（2026-09-24）**：新增
`ASTR_AIR5_CONVECTION_LIMITER=consistent_species`，以两层共享面系数分别
约束流体修正和质量闭合的组分修正，保留双温能量保正，默认不变。
短回放、CPU/GPU、周期守恒与跨 MPI 两层激活、双 rank memcheck 已通过。
三档同物理时间窗口均完成，保存状态有效；入口最大 u 差从旧方案的
`2.51414 / 3.27230` 降为 `0.00116307 / 0.000905875 m/s`。
速度误差有收缩，但不是已验证的三阶时间收敛，痕量组分差也未全部收缩。
推进顺序第 4 项仍未整体关闭，不进入第 5/6 项的长期物理验证。
后续核查组分预算与热力学响应，并补压力平衡、N2 低含量及强激波适用性，
不凭速度误差的改进提前提升生产默认。完整证据见入口审查文档最新章节。

**针对性检查已完成，停在相容通量方案审定点**：新候选的局部短回放温差
主要由对流限制和扩散贡献，不能归为新 GPU 错误或简单归为源项误差。
等压等速、无反应无扩散的小算例中，极小 NO 脉冲经组分限制引发明确温压
漂移；去掉脉冲、或采用 full_state 的控制均保持舍入量级的常量状态。
首 RK 阶段 RHS 与接受状态恒等式确认，组分单独限幅破坏了原通量的恒温
相容性。该候选仍保正和守恒，但不能称为压力平衡通过。
N2 低含量测试也已完成，未出现负组分，但两层系数接近零，不能推广为
低耗散的通用混合物方案。具体参数、12 次小算例及诊断证据见入口审查文档。

下一步先审定质量、组分、携能及压力相容的限制策略，再使用新增小算例
回归，并重放现有 Mach 4 三档窗口。不在尚未确认的能量补偿上直接启动
强激波或长期 SBLI。本轮未修改数值方法和边界条件。

### 已确认的下一候选约束（2026-09-24）

用户确认：保留 N-1 个独立质量分数，指组成满足总和为一后的物理自由度，
不指只存储或推进四个组分。保留现有总密度方程及全部五个组分守恒变量，
不增加第十二个守恒变量。五个组分对称构造候选通量，最终通量联合约束，
不再固定以 N2 余量覆盖状态或对流通量。

- 每个界面最终组分质量通量之和等于该界面的总质量通量。
- 各组分的修正由相邻单元保正预算约束，不再无条件采用所有组分的最小
  系数。相邻单元和 MPI 两侧必须使用同一最终界面通量。
- 不用推进后归一化、截零或浓度地板替代守恒及保正条件。
- 联合检查总能量与振动能的可容许性和通量相容性。质量闭合不等于压力
  平衡，不把逐点补能量当作已验证的修复。
- 固定总质量通量下的可行性条件及失败回退仍需推导。没有可行解时不能
  静默把残差交回 N2；也不能未经推导声称现有低阶流体/组分混合基态保正。
- 审查扩散、化学源项、滤波、边界和重启对组成约束的维护，避免只改对流
后仍由其他路径恢复固定 N2 闭合。保留现有默认路径用于受控对照。

实施顺序：明确联合预算及相容条件，完成 CPU 候选和最小算例，再同步 GPU
并检查 MPI 界面。先验收等压接触输运、痕量幅值扫描和低 N2 工况，随后
进行双温能量检查和既有 SBLI 短回放。所有门槛通过前不提升默认、不启动
长时生产。本节记录已批准方向，不表示联合限制器已实现或验证。

### 设备归约修订与完整小矩阵 memcheck（2026-09-25）

定位链：旧默认 memcheck 通过；新方案初始化后增加显式同步，初始化通过，
低阶预算 kernel 仍非法地址；只把 `sum(ll-lr,dim=2)` 换成 component/direction
定长循环后，接触 NP2 memcheck 通过。预算表达式及外层验收不变。
`cuobjdump --dump-resource-usage` 的 kernel 静态栈前后均 3664 字节，寄存器
均 128。当前证据将问题限定到该归约表达式对应的设备生成代码路径，
不能进一步声称已证明编译器内部缺陷或单纯栈溢出。

修订后重新运行全部 8 组新方案 CPU NP1/NP2、GPU NP2（24 次）：五个 NO
幅值、低 N2、energy、species。全部 GPU NP2 受 memcheck 检查，16 份 rank
日志零错误；每次仍执行数值和共享面检查，未使用 kernel 过滤或错误忽略。
720 对面逐位相同，最大守恒残差 2.54137e-15，自身尺度组分误差 1.57111e-15。
旧默认另 3 次对照及 GPU NP2 memcheck 通过。49 项相关检查器测试通过。

证据：
- 初始化隔离仍失败：`out/air5_symmetric_gpu_memcheck_init_sync_20260925/`。
- 修订后 NO=1e-12：`out/air5_symmetric_gpu_memcheck_scalar_sum_20260925/`。
- 修订后 energy/species：`out/air5_symmetric_gpu_memcheck_stress_fixed_20260925/`。
- 其余痕量、低 N2、默认对照：`out/air5_gpu_scalar_sum_*_20260925/`。

目录均位于 `tests/gpu_validation/`。该结果关闭周期小矩阵的内存门槛，
不关闭物理边界、缓存组成一致性、实际 y/z 分解或 Mach4 回放；完整 goal 继续。

### GPU 周期主循环接入与初次内存门槛失败（2026-09-25，历史记录）

新方案已接 GPU 主时间步：低阶完整 RK 状态、逐轴所有者面投影、同面携能
修正、复用 pinned 面缓冲 MPI 传递、接收侧六面预算及完整 RK 复验。
新选项允许 GPU 周期测试，非周期仍明确拒绝，默认不改。
通用场 halo 最大容量为 9，新预算 11 分量按 6+5 两次连续切片交换，
不修改现有通信流水线或 hm 层数。最终通量独立传递 11 分量及 beta 元数据。
设备子程序输入先收集到线程局部定长数组，避免 NVHPC 跨步切片传参限制。
`chemistry_core_gpu.cuf` 设备函数与调用 kernel 共用现有 128 寄存器上限，
解决 RDC 链接 128/254 冲突；未作性能提升承诺。

数值测试：五个痕量幅值、低 N2、能量/组分压力测试各 CPU NP1/NP2、GPU NP2，
24 次通过；旧 full_state 接触对照另 3 次通过。新路径 720 对完整共享面
逐位一致。最大守恒残差 2.54137e-15，逐组分自身尺度差 1.57111e-15，
统一缩放场差 2.75677e-14。接触最大 p/T/u 漂移分别为
2.47383e-10 Pa / 8.18546e-12 K / 9.52128e-13 m/s。
证据目录 `tests/gpu_validation/out/air5_gpu_owner_matrix_*_20260925/`，
NO=1e-12 接触单独在 `air5_symmetric_gpu_contact_split_20260925/`。

**未通过内存门槛**：`run_air5_layered_stress_gate.py --memcheck` 在新接触
GPU NP2 的首个低阶预算 kernel 同步时报非法地址 700，两 rank 各 1 error。
日志仅包含 API 同步回溯，尚无具体越界访问指令。旧默认同二进制接触 NP2
memcheck 零错误。失败不能忽略，已停止 energy/species sanitizer 及下游验证。
失败目录 `out/air5_symmetric_gpu_memcheck_20260925/`，旧默认控制
`out/air5_default_memcheck_control_20260925/`。
后续先拆分初始化同步点，检查新 kernel 临时数组/设备调用栈与索引，
再重跑失败输入。仅在零错误后扩大内存检查覆盖，不改变数值容差。

### GPU 投影基础件（2026-09-25，先前记录）

设备端有界质量投影和双约束投影已加入 `src_gpu/chemistry_core_gpu.cuf`。
算法与 CPU 一致：增量形式的 243 个活动集枚举、实际操作数舍入误差界、
固定尺寸局部数组、不覆盖指定余量组分。设备端有限值检查使用已有函数。

根 CMake 新目标 `air5_flux_gpu_probe` 通过预处理选项复用 CPU probe 源码，
每次投影实际调用 CUDA kernel，显式同步，比较 CPU/GPU 状态码及通量，
再执行原有数学断言。CPU 单独目标保留。覆盖 1000 个有符号质量投影、
120 种双约束组分置换、正负方向/尺度、痕量、不可行/非有限输入、退化行，
以及此前记录的两个舍入失败输入。两个 probe 均通过；memcheck 零错误。
记录：`tests/gpu_validation/out/air5_flux_gpu_probe_memcheck_20260925.log`。

未接入 GPU RK 主循环，未执行新方案 GPU NP2，因此不得替代完整目标的
CPU/GPU 同相位、MPI 共享、物理边界或 sanitizer 验收。运行时拒绝仍保留。

### 共享最终通量与舍入边界复验（2026-09-25）

CPU 周期候选已采用唯一所有者计算并传递最终通量：每轴本地面
`0:dim-1` 由本 rank 持有，邻居提供面 `-1` 和 `dim`。独立复制的 MPI
communicator 隔离 tag，缓冲区预分配复用；发送 11 个通量及 beta 诊断量。
接收方严格检查自身六面预算，之后检查完整 RK 更新，不平均两侧通量。

整面检查器读取所有方向、三个 stage、各 rank 的发送/接收二进制数据，
按浮点位模式比较，而不是以全场误差容差替代共享一致性。
初次回归发现 energy NP1 在 RK2 内部面 `(24,4,0)` 的振动能余量
为 `-1.21169035041947413e-27`。内部自有面也失败，排除通信接缝为必要条件。
对二分受限 beta 应用已有 `1-gamma128` 算术向内退让并重新投影、严格验证后，
该失败及冻结小矩阵通过。该措施不修改状态，不引入浓度地板，不改变
非受限高阶路径；不能视作对所有后端舍入行为的证明，接收方检查仍保留。

本轮根 CMake 构建通过，49 项相关 Python 检查器测试通过。
实际 ASTR 18 次 CPU 运行：五个 NO 幅值、低 N2、能量/组分压力测试各
NP1/NP2，以及旧 full_state 接触对照 NP1/NP2。新方案 432 对完整面逐位一致，
最大守恒缩放残差 `2.54137e-15`，自身尺度逐组分跨 rank 误差 `1.39655e-15`。
接触及低 N2 的最大 p/T/u 漂移分别为 `2.32831e-10 Pa`、`8.18546e-12 K`、
`9.52128e-13 m/s`。旧默认对照守恒残差最大 `8.70697e-14`。
证据：`tests/gpu_validation/out/air5_owner_matrix_*_20260925/summary.json`，
能量单独证据 `out/air5_symmetric_owner_energy_interior_20260925/summary.json`。
保留初次失败 `out/air5_symmetric_owner_energy_diag_20260925/energy_cpu_np1/run.log`。

尚未关闭 GPU、实际 MPI y/z、物理边界、组分缓存、Compute Sanitizer、
Mach4 SBLI 同 checkpoint 短回放及 dt/2 对照；禁止据此宣称完整移植通过。

### 投影算术修复与小型 CPU 矩阵（2026-09-25，先前记录）

上一轮组分 NP2 的失败已由界面探针定位。RK2 全局界面 23，两侧高阶通量
与预算只有机器量级差异，二分参数却分别为 0.9608154111 和 0.9664230251，
映射总能量通量差约 3.394e-6。原因是高阶输入接近零，保正约束却要求有限
的组分通量重分配；输出中的正负抵消项远大于输入。旧投影以输入尺度检查
输出求和残差，可能要求低于 FP64 可表达精度的闭合，从而对微小输入差异
作出不同的不可行判定。

用该界面的实际操作数加入 `air5_transport_roundoff_probe`，修改前明确失败。
修改后在活跃集、仿射残差迭代及最终原始目标检查中，机器误差界都包含
实际增量/输出求和操作数的绝对值。保留原来的 64 倍机器精度操作界，不使用
浓度地板，不修改单元状态。此处修订的是内部浮点误差估计，接触、保正、
守恒及逐组分跨 rank 的冻结验收容差均未修改。

最新 9 组、18 次实际 CPU 运行全部通过，记录在
`tests/gpu_validation/out/air5_operand_matrix_*_20260925/summary.json`：

- symmetric_species：NO 幅值 0、1e-16、1e-12、1e-8、1e-2 各 NP1/NP2。
- symmetric_species：low_n2、energy、species 各 NP1/NP2。
- full_state：原 NO=1e-12 接触 NP1/NP2，验证旧默认路径对照未受破坏。
- 最大统一缩放周期守恒误差 2.79957e-13；新候选最大逐组分自身尺度
  跨 rank 差 1.22197e-15。组分与能量压力测试均在要求的接口激活已有
  扩散限制器，不能将此表述为所有对流能量约束都已激活覆盖。

在组分 NP2 的 j=k=0 接口抽样，RK2 两侧参数现均为 1，最大最终通量差
约 1.46e-16，处于输入/操作数的舍入尺度；**仍不是位级相同的共享最终通量**。
探针改为每 rank 独立文本文件，避免 MPI 标准输出交错破坏记录。读取器
`check_air5_consistent_residual.py --symmetric-face-log <rank0> <rank1>`
要求所有 rank 记录齐全，并按全局周期界面配对，禁止把缺失记录当作一致。
最新配对报告是组分矩阵目录下的 `faces.json`。初次标准输出交错记录保留
为无效诊断，不用它支撑接口结论。

后续顺序不变：先实现最终界面通量的明确所有权和传递，验证接收通量在
两侧完整 RK 预算中仍可容许，不通过平均单元状态消除差异。随后同步 GPU
实现，处理非周期边界的一侧预算，审查 N2 余量缓存等其他组成处理路径，
再进行 memcheck 与既有 Mach4 短窗口。CPU 矩阵通过不关闭这些任务。
当前 Fortran 基础件及 46 项 Python 针对性测试通过；没有新 GPU 验证。

### CPU 主循环候选与先前失败门槛（2026-09-25）

已实现可选 `symmetric_species`，默认 `full_state` 不变。当前 CPU 实验路径
只接受周期边界；GPU 与非周期边界显式报错，不回退到其他算法。
`chemistry_core.F90:air5_close_species_flux_moment` 对五组分通量进行有界
质量及气体常数加权通量投影，枚举固定规模活跃集，没有指定余量组分。
`chemistry_solver.F90:air5_limit_symmetric_convection` 构造完整 SSPRK3 低阶
状态并交换预算 halo。每个界面的组分修正同步携带振动能及总能量，检查
两侧六面状态的组分非负、密度及双温能量余量。高候选不可容许时，先检查
低阶端点，再对候选参数二分；低阶端点无解则整体报错。尚未证明所有接口
两侧独立重算的最终通量相同，因此不能宣称 MPI 守恒机制已经验收。

两项实际运行暴露的浮点缺陷已修订并保留证据：

1. 横向净 LLF 通量近零时，直接投影的仿射残差未达到原界限。由实际
   i=25,j=0,k=2 失败输入增加 Fortran 回归，使用线性残差迭代修正，
   未放宽投影容差。
2. 直接投影绝对通量会将主体通量舍入残差传给痕量 NO。原接触 CPU NP1/NP2
   的 RK3 原始 NO RHS 最大差约 2.65e-22，限制后达到 1.81e-10，最终 NO
   相对差 6.86e-6。改为求解相对候选的通量增量。原有未修改候选检查已接受
   的机器级仿射残差不再强制分摊给痕量，最终仍按原始两个目标通量检查同一
   容差。这不是修改单元状态、归一化组成或引入浓度地板。

最新二进制在 `out/air5_symmetric_contact_cpu_increment_20260925/astr` 留档。
原 NO=1e-12 接触 CPU NP1/NP2 完整 RK3 通过：最大压力、温度及速度漂移
约 5.82e-11 Pa、2.27e-12 K、3.13e-13 m/s，NO 相对差 7.19e-17。
`out/air5_symmetric_scan_cpu_*_20260925/` 保存其余 NO 幅值
0、1e-16、1e-8、1e-2 及 low_n2 的 NP1/NP2，通过冻结接触、保正、
守恒和逐组分自身尺度比较。低 N2 最大压力/温度/速度漂移分别约
2.33e-10 Pa、8.19e-12 K、9.52e-13 m/s。

`out/air5_symmetric_energy_species_cpu_20260925/` 的 energy NP1/NP2
通过，能量限制确在接口激活，最小系数约 0.21452。但 species NP2
守恒失败，不能把该目录整体标记通过。总能量相对漂移约 -5.34e-8，
超过冻结的 2e-11；动量的统一缩放最大漂移约 6.73e-6。
RK1 近似守恒，RK2 `conv_raw` 的体积分近似闭合，而 `conv` 出现明显
能量及组分不平衡。下一步直接比较 MPI 接口两侧最终通量，建立明确的
共享界面所有权/传递，再检查共享后预算可容许性。不得用放宽容差、平均
单元状态或事后组分重分配掩盖该失败。

当前 Fortran 基础件回归与 45 项 Python 针对性测试通过，不代表 MPI
组分门槛通过。未运行新 GPU、Compute Sanitizer 或 SBLI 回放。另有
原始变量恢复中的 N2 余量质量分数缓存需要在新路径后续审查，不能将本轮
对流通量取消余量覆盖描述为所有组成处理路径已经对称化。

### 联合预算的实现约束与冻结门槛（2026-09-25）

新诊断只读取旧接触测试快照，不推进流场。由首阶段 q 重构显式六阶中心
通量及 LLF 通量，先核对中心通量散度与 ASTR `conv_raw`，再检查界面区间。
检查器使用固定质量截面上的线性加权通量极值，不使用带绝对可行性容差的
通用优化器。五组分采用相同规则，120 种索引置换和方向反转纳入测试。

对等温、等压接触面，除质量闭合外，组分携带的气体常数加权通量也必须
保持相容。固定总质量通量时，仅修正总能量不能同时恢复温度和压力。
现有接触快照的 CPU NP1/NP2、GPU NP2 均显示：界面 25 的逐组分低/高阶
连线区间容许质量闭合，但加权通量上限为约 2499.9999999998877，目标
为 2500，缺口约 1.12e-10，超过本次算术检查界限 7.11e-11。
这是线性约束可行性检查，不是最终物理误差大小的预测。

因此新候选不能只把原来的公共 alpha 换成五个独立 alpha，再投影回这些
连线区间。下一实现采用相邻单元的完整阶段预算构造允许区间：设低阶完整
RK 候选为 U_L，界面修正为 dF，更新系数为 cdt，则每单元的六个面状态为
`V_f = U_L + 6*cdt*sign_f*dF_f`，完整更新是六个 V_f 的平均。
映射坐标下 U_L 使用 Jacobian 加权状态，通量使用相同映射通量。
物种区间由界面两侧 V_f 的非负约束交集给出，而非限制在原低/高阶连线上。
两侧必须使用同一个最终通量。密度、振动能余量和去除动能后的平动能余量
仍须在同一 V_f 上检查，不能由质量区间可行推断双温可行。

在已保存的接触和低 N2 首阶段，六面预算区间均允许固定高阶质量通量及
气体常数加权通量约束。接触界面 25 的加权通量允许范围约为
2028.679--2963.670，包含 2500。这个较宽区间仅给出可行域，不能直接选
任意可行点，应选靠近原候选的修正，并联合约束携能通量。
一般非均匀温度、强激波及后续 RK 阶段尚未验证。无可行解时不得转移残差
给 N2、截零或静默切换未证明的流体/组分混合基态。

新主循环候选运行前冻结以下门槛，不用后续结果反向调整容差：

| 项目 | 输入与门槛 |
| --- | --- |
| 接触平衡 | 既有 48x6x6 周期区间，p=1e5 Pa、T=4000 K、Tv=1500 K、u=100 m/s，dt=5e-9 s，完整 RK3；最大 p/T/u 漂移分别 <=1e-7 Pa、1e-9 K、1e-10 m/s |
| 痕量扫描 | 接触 NO 质量分数 0、1e-16、1e-12、1e-8、1e-2；这些是输入幅值，不是数值地板；同样检查接触门槛 |
| 低 N2 | 沿用既有 low_n2 组成与网格；同样检查接触、保正和守恒门槛 |
| 组分对称性 | 约束算子五组分及对应热力学系数联合执行全部 120 种索引置换；不可只交换浓度而保持物性索引来宣称物理不变 |
| 保正与双温 | 每个 pre_rhs/post_update 阶段 rho>0、全部部分密度>=0，振动能余量>=0，T/Tv 在当前物性适用域；不得截零/归一化后再检查 |
| 质量与守恒 | 沿用组成闭合 atol=1e-14、rtol=2e-12；去重周期体积分 11 分量缩放漂移<=2e-11；同时报告各组分自身尺度的误差，不以主体尺度掩盖痕量 |
| CPU/GPU/MPI | CPU NP1、CPU NP2、GPU NP2，同相位全场沿用 atol=1e-9、rtol=1e-10；额外逐组分最大误差/初始该组分最大部分密度<=1e-9，初始严格为零的组分不得产生非零输运；两侧最终界面通量也需核对 |
| 实际约束激活 | 保留 energy/species 冻结测试，确认约束在要求的 MPI 接口激活；不能仅跑不触发限制器的光滑场 |
| 内存安全 | 修改路径 Compute Sanitizer memcheck 零错误 |
| SBLI 短回放 | 既有 Mach4 checkpoint、相同物理时间与 dt/2 控制，沿用现有窗口契约和判据；失败时停止后续长时验证，不提升默认 |

证据位于 `tests/gpu_validation/out/air5_consistent_residual_20260924/` 的
`contact_face_feasibility_*.json` 与 `low_n2_face_feasibility_cpu.json`。
当前仅为冻结门槛和约束诊断；没有新 RK 候选通过记录。

### 修复第一步：时序审查与 CPU 质量闭合基础件（2026-09-25）

用户已批准开始修复。当前静态审查未发现 OpenCFD 中所述的逐组分更新后再
计算下一组分 RHS 问题：CPU 在 `rhscal` 返回后执行所有分量的 RK 更新；
GPU 在三向对流及扩散完成后执行统一 RK kernel，并有显式同步。此结论限于
已审查的输运调用顺序，不等于所有并发/边界路径的动态检查。未新增状态快照。

在 `src/chemistry_core.F90` 的 `chemistry_model` 中增加纯 FP64 例程
`air5_close_species_flux`，求候选界面通量到给定五组分区间及总质量约束的
欧氏投影。各组分使用相同规则，不指定 N2，不修改任何单元状态。无解、非有限
输入或未达到浮点闭合要求时返回非成功状态，并保持输出为原候选。
区间必须由调用方提供，本例程**不构造完整阶段保正预算、不约束双温能量，
也不保证压力平衡**。当前没有接入 CPU/GPU 主循环，默认和已有候选行为不变。

通过顶层 CMake 更新 `build_cpu_probe`，NVHPC 26.1 编译并运行扩展后的
`air5_transport_roundoff_probe`。测试覆盖痕量负候选的界面预算修正、主要组分
修正幅度、循环组分置换、面方向反转、二进制尺度变化、零通量、非法区间、
NaN 和无解拒绝，以及 1000 组有界有符号通量的约束与投影最优性检查。
既有 41 项针对性 Python 测试也通过。没有执行新的流场或 GPU 内存检查。

复用旧接触算例快照，压力平衡断言仍失败（1.31117 Pa / 0.443826 K），作为
未修复的明确门槛。下一步是建立共享单元预算和能量相容约束，再接入实际
ASTR 候选并重复该门槛；不可将此基础件测试标记为通量缺陷已修复。
